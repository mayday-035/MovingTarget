<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MovingTarget" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Gunmetal_Gray</item>
        <item name="colorPrimaryVariant">@color/Ash_Gray</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/bg_gray</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/Gunmetal_Gray</item>
        <!--Background color-->
        <item name="android:windowBackground">@color/Light_Gray</item>
        <!-- Text color-->
        <item name="android:textColor">@color/black</item>
        <item name="tint">@color/black</item>
        <item name="android:popupBackground">@color/Light_Gray</item>
        <!-- Customize your theme here. -->
    </style>
</resources>